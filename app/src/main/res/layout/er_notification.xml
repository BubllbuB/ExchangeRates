<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        xmlns:tools="http://schemas.android.com/tools"
        android:gravity="center"
        android:background="@drawable/er_backdrop_bg"
        android:padding="10dp">

    <ImageView
            android:id="@+id/image_notification"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="10dp"
            android:padding="10dp"
            android:adjustViewBounds="true"
            android:background="@drawable/er_notification_bg"
            android:scaleType="center"
            app:srcCompat="@drawable/ic_notification"
            android:contentDescription="@string/contentDescriptionNotificationIcon" />

    <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_gravity="center_vertical"
            android:orientation="vertical">

        <TextView
                android:id="@+id/titleNotification"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text=""
                android:textSize="14sp"
                android:textColor="#fff"
                tools:text="Tap"/>

        <TextView
                android:id="@+id/subTitleNotification"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/notificationSubTitle"
                android:textSize="12sp"
                android:textColor="#fff"
                tools:text="Tap to show chart"/>
    </LinearLayout>
</LinearLayout>